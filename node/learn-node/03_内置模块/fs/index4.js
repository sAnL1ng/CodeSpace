const fs = require('fs')

// fs.appendFileSync('./data.txt',' 不教Human度M3') // 后面追加
// fs.writeFileSync('./data.txt','不教Human度M3') // 重写覆盖

// fs.renameSync('./data.txt','./data.md') // 修改文件名
// fs.renameSync('./pic/data.md','./data.txt') // 移动文件

// fs.unlinkSync('./data.txt') // 删除文件
// fs.rmSync('pic',{ recursive: true}) // 删除该目录且包含所有文件
