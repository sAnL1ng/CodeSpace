import React, { Component } from 'react'

export default class todoItem extends Component {
    const list = array.map((item,index) => <li></li>)

  render() {
    return (
      <ul>
        <li ></li>
      </ul>
    )
  }
}
