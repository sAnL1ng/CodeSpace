let s = 'abcdefg'

// let h = '123'
// let g = s.concat(h)
// s = s.padStart(5,'0') // 前面补全
// s = s.padEnd(5,'0') // 后面补全
// c·nsole.log(g,s,h);

// console.log(s.substr(2,3)); // 第一个参数:起始下标 第二个canshu:截取的长度

// console.log(s.substring(2,3)); //第一个参数：起始下标 第二个参数：结束下标 左闭右开

// replace 返回被修改后的字符串 原字符串不受影响
// let s2 = s.replace(/c/g,'0')

// repeat
// s.repeat()

// console.log(s.toLowerCase);

console.log(s.split('d'));